rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Broadly authorize any authenticated user to read and write any document.
    // This supports the "All Data Merge" community model where shepherds share a collaborative database.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}