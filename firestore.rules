rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global Collaborative Access: 
    // Allows anyone to read or write any document in the database.
    // This supports a fully shared real-time farm management ecosystem.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}